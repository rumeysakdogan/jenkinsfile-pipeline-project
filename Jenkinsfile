pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo 'Practising jenkins jobs and pipelines'
                sh 'echo Integrating Jenkins Pipeline with GitHub Webhook using Jenkinsfile'
            }
        }
    }
}
